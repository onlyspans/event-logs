apiVersion: v1
kind: Secret
metadata:
  name: {{ include "event-logs.fullname" . }}
  labels:
    {{- include "event-logs.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
